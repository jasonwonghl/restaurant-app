import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  button: {
    position: 'absolute',
    right: 10,
    width: 30,
    height:30,
    zIndex: 10
  },
  buttonImage: {
    width: 30,
    height:30
  }
})
